services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: deview
      POSTGRES_PASSWORD: deview
      POSTGRES_DB: deview
    ports:
      - "5432:5432"
    volumes:
      - deview_pg:/var/lib/postgresql/data

volumes:
  deview_pg:
