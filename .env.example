# Required for AI case summaries (ChatGPT). Paste your secret key in .env.local — never commit real keys.
OPENAI_API_KEY=

# Optional: defaults to gpt-4o-mini
# OPENAI_MODEL=gpt-4o-mini

# Demo login password for MVP (override in production)
PORTAL_DEMO_PASSWORD=deview-demo
